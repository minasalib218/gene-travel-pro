"use client";

import type { DayPlan, SelectedRecommendations } from "@/lib/recommendation/types";

export function ContinueFooter({ hasSelections, selected, dayPlan, submitting, onContinue, onEdit }: { hasSelections: boolean; selected: SelectedRecommendations; dayPlan: DayPlan[]; submitting: boolean; onContinue: () => void; onEdit: () => void }) { return <div className="fixed bottom-0 left-0 right-0 z-50 border-t border-white/10 bg-black/70 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-4 md:flex-row md:items-center md:justify-between"><div className="flex flex-wrap items-center gap-2 text-sm text-white/70"><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1">{selected.hotel ? "Hotel selected" : "No hotel yet"}</span><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1">{selected.flight ? "Flight selected" : "No flight yet"}</span><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1">{selected.activities.length} activities</span><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1">{dayPlan.length} day plan</span></div><div className="flex gap-3"><button onClick={onEdit} className="rounded-2xl border border-white/10 bg-white/5 px-5 py-3 text-sm text-white/80 transition hover:bg-white/10 hover:text-white">Edit selections</button><button disabled={!hasSelections || submitting} onClick={onContinue} className="rounded-2xl px-5 py-3 text-sm font-semibold text-black transition disabled:cursor-not-allowed disabled:opacity-50" style={{ backgroundColor: "#ff7a00" }}>{submitting ? "Continuing..." : "Continue to Analysis"}</button></div></div></div>; }
